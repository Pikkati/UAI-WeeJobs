# Lottie Assets

This folder is reserved for animation JSON sources.

## Rules

- commit the `.json` animation file here
- commit a static fallback under `assets/lottie/fallbacks/`
- add manifest entries for both the animation and fallback
- note reduced-motion behavior in the manifest and PR description

## Current state

No production Lottie assets are currently registered in the repo.
