# Lottie Fallbacks

Store static fallback images here for animations shipped under `assets/lottie/`.

Recommended naming:

- `lottie-job-posting-success.png`
- `lottie-empty-inbox.png`

Every fallback should be linked to a matching manifest entry and corresponding animation source.
